import { TagsManagerDirective } from './tags-manager.directive';

describe('TagsManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new TagsManagerDirective();
    expect(directive).toBeTruthy();
  });
});
